<%= form_with(model: probando, local: true) do |form| %>
  <% if probando.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(probando.errors.count, "error") %> prohibited this probando from being saved:</h2>

      <ul>
      <% probando.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :user_id %>
    <%= form.text_field :user_id %>
  </div>

  <div class="field">
    <%= form.label :post_id %>
    <%= form.text_field :post_id %>
  </div>

  <div class="field">
    <%= form.label :parragraph %>
    <%= form.text_field :parragraph %>
  </div>

  <div class="field">
    <%= form.label :number %>
    <%= form.number_field :number %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
